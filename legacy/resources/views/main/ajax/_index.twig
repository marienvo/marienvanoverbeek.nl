{% for project in projects %}
    {% if project.id == url_segment[1] %}
        {% include 'global/blocks/project.twig' with {
            'project_id': project.id,
            "data": project.data,
            "index": loop.index-1,
            "length": loop.length-1
        } %}
    {% endif %}
{% endfor %}